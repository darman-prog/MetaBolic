import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { MetabolicButtonComponent } from '../../shared/components/metabolic-button/metabolic-button.component';
import { MetabolicCardComponent } from '../../shared/components/metabolic-card/metabolic-card.component';
import { StatusBadgeComponent } from '../../shared/components/status-badge/status-badge.component';

@Component({ selector: 'metabolic-protocols', standalone: true, imports: [MetabolicButtonComponent, MetabolicCardComponent, StatusBadgeComponent], changeDetection: ChangeDetectionStrategy.OnPush, template: `<section class="page"><header class="heading"><div><p class="eyebrow">OPERATOR_042 // PROTOCOL_LIBRARY</p><h1>PROTOCOLOS_OPERATIVOS</h1><p>Gestión de rutinas tácticas y optimización de sistemas biológicos.</p></div><button metabolicButton type="button" (click)="creator.set(!creator())">+ NUEVO_PROTOCOLO</button></header>@if(creator()){<metabolic-card accent="rose"><div class="creator"><p class="eyebrow">NEW_PROTOCOL.sh</p><input aria-label="Designacion del protocolo" placeholder="EJ. PROTOCOLO_OMEGA" /><button metabolicButton variant="outline" type="button" (click)="creator.set(false)">COMPILAR_PROTOCOLO</button></div></metabolic-card>}<div class="protocol-grid">@for(protocol of protocols; track protocol.name){<metabolic-card><div class="card-top"><status-badge [label]="protocol.status" [tone]="protocol.status === 'ALPHA' ? 'rose' : 'cyan'" /><span class="eyebrow">{{protocol.id}}</span></div><h2>{{protocol.name}}</h2><div class="data-grid"><span>MODULOS <b>{{protocol.modules}}</b></span><span>DURACION <b>{{protocol.duration}} MIN</b></span><span>CARGA <b>{{protocol.calories}} KCAL</b></span></div><button metabolicButton variant="outline" type="button">INITIALIZE_SEQUENCE →</button></metabolic-card>}</div></section>`, styles: [`:host{display:block}.page{display:grid;gap:24px}.heading{display:flex;justify-content:space-between;align-items:end;gap:20px}.eyebrow{margin:0;color:var(--metabolic-primary);font:700 10px Space Mono,monospace;letter-spacing:.15em}.heading h1{margin:10px 0;color:var(--metabolic-text);font:600 clamp(1.6rem,4vw,2.4rem) Orbitron,sans-serif;letter-spacing:.04em}.heading p:not(.eyebrow){margin:0;color:var(--metabolic-text-muted);font:14px/1.6 Space Mono,monospace}.protocol-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.card-top{display:flex;justify-content:space-between;align-items:center}.card-top .eyebrow{color:var(--metabolic-text-dim)}h2{margin:24px 0;color:var(--metabolic-text);font:600 18px Orbitron,sans-serif}.data-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;border-block:1px solid var(--metabolic-grid-line);padding:16px 0;margin-bottom:20px;color:var(--metabolic-text-muted);font:9px Space Mono,monospace}.data-grid b{display:block;margin-top:8px;color:var(--metabolic-primary);font-size:13px}.creator{display:flex;gap:16px;align-items:center}.creator input{flex:1;padding:13px;background:var(--metabolic-surface-dark);border:1px solid var(--metabolic-grid-line);color:var(--metabolic-text);font:12px Space Mono,monospace}.creator .eyebrow{white-space:nowrap;color:var(--metabolic-secondary)}@media(max-width:900px){.protocol-grid{grid-template-columns:1fr 1fr}}@media(max-width:600px){.heading,.creator{align-items:stretch;flex-direction:column}.protocol-grid{grid-template-columns:1fr}}`] })
export class ProtocolsComponent { readonly creator = signal(false); readonly protocols = [{ id: 'PX-08', name: 'Protocolo Hipertrofia', status: 'ALPHA' as const, modules: 8, duration: 75, calories: 620 }, { id: 'PX-05', name: 'Resistencia Metabolica', status: 'STABLE' as const, modules: 5, duration: 55, calories: 340 }, { id: 'PX-12', name: 'Movilidad Tactica', status: 'STABLE' as const, modules: 12, duration: 45, calories: 290 }]; }
