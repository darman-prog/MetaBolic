import { Routes } from '@angular/router';
import { BiometricSyncComponent } from './biometric-sync.component';
export const BIOMETRIC_ROUTES: Routes = [{ path: '', component: BiometricSyncComponent }];
