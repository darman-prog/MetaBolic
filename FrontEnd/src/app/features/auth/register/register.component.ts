import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { NonNullableFormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { MetabolicButtonComponent } from '../../../shared/components/metabolic-button/metabolic-button.component';
import { MetabolicCardComponent } from '../../../shared/components/metabolic-card/metabolic-card.component';
import { ScanlineComponent } from '../../../shared/components/scanline/scanline.component';
import { TerminalInputComponent } from '../../../shared/components/terminal-input/terminal-input.component';
import { AuthService } from '../../../shared/services/auth.service';

@Component({ selector: 'metabolic-register', standalone: true, imports: [ReactiveFormsModule, RouterLink, MetabolicButtonComponent, MetabolicCardComponent, ScanlineComponent, TerminalInputComponent], changeDetection: ChangeDetectionStrategy.OnPush, template: `<main class="register-page"><metabolic-scanline /><aside class="corner corner--left">SYS_LOG_ACTIVE<br /><span>NET: SECURE_V3</span></aside><aside class="corner corner--right">LOC: LAT_UNDEFINED<br /><span>STATUS: NOMINAL</span></aside><section class="register-panel fade-enter"><metabolic-card accent="cyan"><header><div class="terminal-icon" aria-hidden="true">⌘</div><h1>REGISTRO_NUEVO_AVATAR</h1><p>// PROTOCOLO_INGRESO</p><small>SISTEMA OPERATIVO BIOMETRICO v4.0.2</small></header><form [formGroup]="form" (ngSubmit)="submit()" novalidate><terminal-input label="ID_OPERADOR" type="email" placeholder="CORREO_ELECTRONICO" formControlName="email" /><terminal-input label="CODIGO_ACCESO" type="password" placeholder="CLAVE_ENCRIPTADA" formControlName="password" /><terminal-input label="CONFIRMAR_CODIGO" type="password" placeholder="REINGRESAR_CLAVE" formControlName="confirmPassword" />@if (form.errors?.['passwordMismatch'] && submitted()) {<p class="api-error" role="alert">CONFIRMAR_CODIGO // LOS VALORES NO COINCIDEN.</p>}@if(error()){<p class="api-error" role="alert">{{error()}}</p>}<button metabolicButton type="submit" [disabled]="loading()">{{loading() ? 'RECRUITING...' : 'RECLUTAR_AHORA'}} <span aria-hidden="true">→</span></button></form><div class="divider"><span>O_SINCRONIZAR_MEDIANTE</span></div><button metabolicButton variant="outline" type="button" (click)="googleSync()">◉ Google Sync</button><p class="login-link">¿YA TIENES CREDENCIALES? <a routerLink="/auth/login">INICIAR_SESION</a></p></metabolic-card></section></main>`, styles: [`:host{display:block}.register-page{min-height:100dvh;display:grid;place-items:center;padding:24px;position:relative}.register-panel{width:min(100%,480px)}header{text-align:center;border-bottom:1px solid var(--metabolic-grid-line);padding-bottom:18px;margin-bottom:24px}.terminal-icon{color:var(--metabolic-primary);font-size:36px;text-shadow:0 0 8px var(--metabolic-glow-cyan)}h1{margin:0;color:var(--metabolic-primary);font:600 clamp(1.25rem,4vw,1.55rem)/1.2 Orbitron,sans-serif;letter-spacing:.1em}header p{margin:10px 0 16px;color:var(--metabolic-text-muted);font:700 11px/1 Space Mono,monospace;letter-spacing:.2em}header small{color:var(--metabolic-text-dim);font:10px/1 Space Mono,monospace;letter-spacing:.1em}.register-panel form{display:grid;gap:18px}.divider{display:flex;align-items:center;gap:12px;margin:24px 0;color:var(--metabolic-text-muted);font:10px Space Mono,monospace;letter-spacing:.08em}.divider:before,.divider:after{content:'';height:1px;flex:1;background:var(--metabolic-outline-variant)}.login-link{margin:24px 0 0;text-align:center;color:var(--metabolic-text-muted);font:10px/1.5 Space Mono,monospace;letter-spacing:.06em}.login-link a{color:var(--metabolic-primary);text-decoration:underline;text-underline-offset:4px}.corner{position:fixed;top:40px;color:var(--metabolic-primary);font:10px/1.8 Space Mono,monospace;letter-spacing:.1em;opacity:.6}.corner span{color:var(--metabolic-text-muted)}.corner--left{left:40px}.corner--right{right:40px;text-align:right}@media(max-width:700px){.corner{display:none}.register-page{padding:20px 14px}}`] })
export class RegisterComponent { private readonly fb = inject(NonNullableFormBuilder); private readonly auth = inject(AuthService); private readonly router = inject(Router); readonly loading = signal(false); readonly error = signal(''); readonly submitted = signal(false); readonly form = this.fb.group({ email: ['', [Validators.required, Validators.email]], password: ['', [Validators.required, Validators.minLength(8)]], confirmPassword: ['', Validators.required] }, { validators: control => control.get('password')?.value === control.get('confirmPassword')?.value ? null : { passwordMismatch: true } }); submit(): void { this.submitted.set(true); this.error.set(''); if (this.form.invalid) { this.form.markAllAsTouched(); return; } this.loading.set(true); const { email, password } = this.form.getRawValue(); this.auth.register({ email, password }).subscribe({ next: () => { this.loading.set(false); void this.router.navigate(['/biometric-sync']); }, error: () => { this.loading.set(false); this.error.set('REGISTRATION_FAILURE // NO SE PUDO RECLUTAR EL AVATAR.'); } }); } googleSync(): void { this.error.set('EXTERNAL_VALIDATION // GOOGLE SYNC NO CONFIGURADO.'); } }
