import { ChangeDetectionStrategy, Component, OnDestroy, OnInit, inject, signal } from '@angular/core';
import { Router } from '@angular/router';
import { ProgressBarComponent } from '../../../shared/components/progress-bar/progress-bar.component';
import { ScanlineComponent } from '../../../shared/components/scanline/scanline.component';

@Component({ selector: 'metabolic-boot', standalone: true, imports: [ScanlineComponent, ProgressBarComponent], changeDetection: ChangeDetectionStrategy.OnPush, template: `<main class="boot"><metabolic-scanline /><div class="boot__logo pulse-fast"><svg viewBox="0 0 100 100" aria-hidden="true"><path d="M50 10 80 30v40L50 90 20 70V30Z M10 50h25l10-30 10 60 10-30h25" /></svg></div><section class="boot__status"><div class="boot__percent font-data">[ <span>{{ progress() }}%</span> ]</div><progress-bar [value]="progress()" label="Progreso de inicializacion" /><p aria-live="polite">{{ status() }}</p></section><aside class="telemetry telemetry--left" aria-hidden="true">01010101010101010<br />INITIALIZING_BOOT_SEQUENCE...<br />KERNEL_STABLE: <b>OK</b><br />CORE_VOLTAGE: 1.2V<br />NEURAL_LOAD: 12%</aside><aside class="telemetry telemetry--right" aria-hidden="true">LATENCY: 4MS<br />PKT_LOSS: 0%<br />ENCRYPT: AES_256<br />SIG_STRENGTH: 98%<br />UPLINK: ACTIVE</aside></main>`, styles: [`:host{display:block}.boot{min-height:100dvh;display:grid;place-items:center;position:relative;padding:24px}.boot__logo{width:224px;height:224px;color:var(--metabolic-primary);filter:drop-shadow(0 0 12px var(--metabolic-primary))}.boot__logo svg{width:100%;height:100%;fill:none;stroke:currentColor;stroke-width:2}.boot__logo path{stroke-width:3;stroke-linecap:round;stroke-linejoin:round}.boot__status{width:min(100%,500px);margin-top:320px;text-align:center;display:grid;gap:20px}.boot__percent{color:var(--metabolic-primary);font-size:28px;font-weight:700;letter-spacing:.16em;text-shadow:0 0 10px var(--metabolic-glow-cyan)}.boot__status p{height:22px;margin:0;color:var(--metabolic-primary);font:14px/1.5 Space Mono,monospace;letter-spacing:.12em}.telemetry{position:fixed;opacity:.45;color:var(--metabolic-primary);font:10px/2 Space Mono,monospace}.telemetry b{color:var(--metabolic-tertiary)}.telemetry--left{top:32px;left:32px}.telemetry--right{right:32px;bottom:32px;text-align:right}@media(max-width:700px){.boot__status{margin-top:0}.telemetry{display:none}}`] })
export class BootComponent implements OnInit, OnDestroy { private readonly router = inject(Router); readonly progress = signal(0); readonly status = signal('INICIANDO SECUENCIA...'); private timer?: ReturnType<typeof setInterval>; private readonly messages = ['INICIANDO SECUENCIA...', 'SINCRONIZANDO PROTOCOLOS...', 'VALIDANDO NEURAL LINK...', 'KERNEL_STABLE: OK', 'UPLINK: ACTIVE']; ngOnInit(): void { this.timer = setInterval(() => { const next = Math.min(this.progress() + 10, 100); this.progress.set(next); this.status.set(this.messages[Math.min(Math.floor(next / 25), this.messages.length - 1)]); if (next === 100) { this.clear(); void this.router.navigate(['/auth/login']); } }, 180); } ngOnDestroy(): void { this.clear(); } private clear(): void { if (this.timer) clearInterval(this.timer); } }
